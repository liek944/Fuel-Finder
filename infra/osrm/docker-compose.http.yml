version: "3.8"

services:
  osrm:
    image: osrm/osrm-backend:latest
    container_name: osrm
    restart: unless-stopped
    command: >
      osrm-routed --algorithm mld /data/region.osrm
    volumes:
      - ./data:/data
    ports:
      - "5000:5000"
